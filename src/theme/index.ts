/**
 * Polly Theme exports
 * 
 * Usage:
 *   import { colors, typography, spacing } from '@/theme';
 *   
 * Or for a component:
 *   import theme from '@/theme/colors';
 */

export {
  colors,
  typography,
  spacing,
  layout,
  semanticColors,
} from './colors';
