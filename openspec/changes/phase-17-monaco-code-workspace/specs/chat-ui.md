# Chat UI (design reference – Phase 17 change)

**Scope:** Incremental improvements to Polly's chat so it better matches Cursor/Void patterns. No fork code; design reference only.

- **Layout:** Sidebar or panel placement, model/persona visibility (Cursor/Void as reference).
- **Context:** Optional "code context" pills (current file, selection) when in Code view; include in prompt or display.
- **Implementation:** Reuse existing chat components; style and layout tweaks. Can be same release as Phase 17 or a follow-up change.

See [../tasks.md](../tasks.md) section "Chat UI refresh (design reference)."
