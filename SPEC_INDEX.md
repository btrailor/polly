# Polly Spec Index

Single source of truth for all spec documents. A spec doesn't officially exist until it's listed here.

---

## How This Works

- **All specs live at `~/polly/` root** — flat, no subdirectories
- **Naming convention:** `SCREAMING_SNAKE_CASE.md` (e.g. `VOICE_INTERACTION.md`)
- **A spec is "active" when:** it's listed here with a phase and status
- **A spec is "reference" when:** it informs decisions but isn't directly implemented (e.g. philosophy docs)
- **Before writing a new spec:** add it here first with status `planned`, then write it

---

## Active Specs

| Spec | Phase | Status | What it covers |
|------|-------|--------|----------------|
| `POLLY_IOS_SPEC.md` | 1–4 | ✅ Authoritative | Full iOS feature spec — canonical source of truth |
| `POLLY_AGENT_TEMPLATES.md` | 1–4 | ✅ Authoritative | 31 agent templates, team rosters, SOUL content |
| `VOICE_INTERACTION.md` | 1 | 🔨 In progress | Voice requirements: idle timer, mic button prominence, interruption recovery |
| `COPY_VOICE.md` | 1 | 🔨 In progress | Voice design tokens, state machine, copy patterns, accessibility |
| `VOICE_BEHAVIOR_TESTS.md` | 1 | 🔨 In progress | QA test suite for voice interaction |
| `PROFESSOR_STUDIES_MODE.md` | ? | ⚠️ Unresolved | Referenced in `chat.tsx` file header spec comment — no file exists, not yet indexed. Needs Brett to confirm: is this a planned spec or a stale reference to remove? |
| `FIGMA_INTEGRATION.md` | 2 | 📋 Planned | Figma link-surfacing in chat; deep-link to boards from spec context |
| `PROJECT_STATUS.md` | 1 | 📋 Draft | Living project status doc — cross-conversation memory for the dev team. @code_architect writes, all agents read-only. Tracks shipped items, active work, spec status, open blockers, git state. |
| `AGENT_BOOTSTRAP_DEV.md` | 1 | 📋 Draft | Dev group chat bootstrap sequence — git state verify, PROJECT_STATUS read, spec file existence check, SPEC_INDEX read, declare ready. Agent-specific startup reads table. 5 hard rules. 6 failure modes prevented. |
| `TREE_OF_THOUGHTS.md` | 3 | 💡 Idea | ToT as activatable reasoning mode under §11 (mental models); secondary hook: plan-space search/goal decomposition. Composable with existing cognitive lenses. Ref: https://github.com/kyegomez/tree-of-thoughts |
| `TEMPORAL_INTELLIGENCE.md` | 3 | 💡 Idea | Drift detection + Drift view — semantic diff of user positions over time. Two-pipeline architecture: Knowledge Skill ingests all content; Temporal layer runs idle async pass (classifier filter → LLM position extraction → separate position index). Topic ontology is emergent (cluster position embeddings, not fixed taxonomy — Freirean: categories from user's own knowledge). Drift view = topic map of intellectual stances with time axis. Dialogic agent interaction on detected drift. Prior art: diachronic word embeddings (Hamilton et al. 2016) — Polly's contribution is applying this to personal intellectual history. Depends on Knowledge Skill being live. |
| `LOCKDOWN_MODE.md` | 3 | 💡 Idea | Computational sovereignty mode — "can't" not "don't." No APNs, no Cloudflare Tunnel, no relay, no mDNS. LAN/Tailscale only. `NSFileProtectionComplete` on all session files. Memory-only voice buffers (never written to disk). Gateway: encrypted SQLite/flat files with key in macOS Secure Enclave. Duress/panic wipe. Session memory zeroed by default. Community+cloud skills disabled. Audit log tamper-evident. Operational security guide as much as architecture doc. **Co-owners: @code_architect + @security_audit.** Architectural decisions inform Phase 1/2 — not retrofittable. |
| `FEDERATED_COLLABORATION.md` | 3 | 💡 Idea | Cross-instance Liaison protocol — two sovereign Polly stacks collaborating without a central server. Peer discovery via mDNS/Tunnel. Symmetric TOFU pairing (neither instance is primary). Liaison agents communicate via async mailbox across gateways using a shared git repo as transport (append-only, fork-at-any-time, signed commits, no platform dependency). Each Liaison exposes a filtered *projection* of its owner's context — not full vault access. Revocation instant and unilateral. Architecturally enforces horizontal structure (Graeber): no central authority, no owner, forkable at any time. Extends §22.9 Tier 2 cross-gateway. Monome/OSC model: sovereign instruments creating shared resonance space. |
| `METACOGNITIVE_DASHBOARD.md` | 3 | 💡 Idea | Reasoning habit mirror — tracks which mental models (§11.3) the user applies, with what frequency, to which agents, with what outcome signals (implicit: task creation, vault writes, conversation resolution; explicit: user rating). Outcome correlation surfaces empirical data about reasoning effectiveness (e.g., Inversion+Strategist → high signal for this user). `suggested_models` field in agent templates dynamically re-ranked based on personal effectiveness history. Not a judgment system — a mirror. All data local-only, never leaves gateway, explicitly deletable. Depends on §11.3 mental models live. Connects to TEMPORAL_INTELLIGENCE.md (different axis: reasoning patterns vs. position drift). Extends old PatternLearner concept from §A.6 salvage audit, elevated to reasoning-decision layer. |
| `CREATIVE_CONSTRAINT_ENGINE.md` | 3 | 💡 Idea | Oblique Strategies as system layer — agents reshape the *structure of engagement*, not just output style. Constraints provoke thinking rather than limit it (Bogost: constraints create meaning). Targeted defamiliarization using Metacognitive Dashboard data (knows your reasoning habits → deploys constraints that complete them, not fight them). Sensibility structural layer: Ghost Box = fragment mode (refuses complete answers, leaves gaps); Riley = systematic variation mode (forced assumption changes). Two Sensibility layers: (1) aesthetic (current — tone/style), (2) structural (new — engagement rules, what agent refuses to answer directly). SOUL-level implementation, not skill/infrastructure. @design_eng: structural rules need a home in Sensibility documentation alongside aesthetic tokens. |
| `PRACTICE_LAYER.md` | 3 | 💡 Idea | Practice mirror alongside Plans (§23) — infinite games frame. Practices have no completion state; they have frequency, depth, and cross-domain connection density. Data model: `last_engaged`, `engagement_frequency` (90-day histogram), `depth_signal` (link density/word count), `cross_domain_links` (wikilink counts between practice domains), configurable `dormancy_threshold_days`. Most data derived from Knowledge Skill's existing wikilink graph + file timestamps — no new user input required. Dormancy alert: "Glyphs has gone quiet — 6 weeks." Cross-pollination view: which domain pairings are active, siloed, historically generative. Companion to Plans, not replacement. Depends on Knowledge Skill (wikilink graph layer) + user-defined domain taxonomy in vault. |
| `ORAL_HISTORY.md` | 3 | 💡 Idea | Voice-first longitudinal self-documentation. Daily reflections with Mentor (or any agent) accumulate as a transcribed oral history corpus. Annual synthesis pass by a new Archivist agent produces an "intellectual autobiography" — recurring themes, decision points, unresolved questions, position shifts (Temporal Intelligence data), practice rhythms (Practice Layer data). Output: structured markdown (Scrolls-compatible), publishable or private. Archivist agent template needed in POLLY_AGENT_TEMPLATES.md — longitudinal synthesis register, slower/more reflective than Mentor. No new infrastructure — new pointing direction for voice transcription + Knowledge Skill + multi-session synthesis. Depends on: Knowledge Skill (corpus indexing), voice transcription (Phase 1). Enriched by: Temporal Intelligence, Practice Layer. |
| `EPISTEMIC_IMMUNE_SYSTEM.md` | 3 | 💡 Idea | Adaptive constitutional layer — not what it believes (fixed principles), but *what it notices* (learned pattern recognition). Accumulates a library of rhetorical patterns that have successfully bypassed Brett's critical faculties. When a new argument matches a known pattern: "This has the same structure as the argument from February that you accepted without examining the funding source — the Contrarian later pointed out Y." Biological metaphor is precise: not a firewall, a targeted antibody response. Library built from Contrarian session outcomes (where Contrarian successfully corrected an accepted argument → extract rhetorical structure). Data: rhetorical structure, emotional register, domain patterns, specific historical instances. Privacy is load-bearing: a map of your rhetorical vulnerabilities must stay local. Only possible because Polly has longitudinal memory + epistemological framework + self-hosted privacy guarantee — no other product has all three. Extension of constitutional layer injection point, not new infrastructure. Depends on: constitutional layer live, Contrarian agent active, Knowledge Skill corpus. CL4R1T4S_RESEARCH.md is ready-made starting vocabulary for the pattern library. Flags for examination, never blocks. Must be legible — show exactly which pattern was recognized and why. |
| `STRUCTURAL_ANALOGY.md` | 3 | 💡 Idea | Cross-domain structural transfer from personal corpus — "find the version of me that already solved this problem in a different domain." Semantic RAG finds surface similarity; structural analogy finds shape similarity. Grounded in Gentner's Structure Mapping Theory (1983): analogical reasoning aligns relational structure, not surface features. Three indexing layers: (1) semantic embedding (existing), (2) structural pattern tags (new — controlled taxonomy ~30-50 patterns: layered-dependency, feedback-loop, constraint-produces-emergence, etc.), (3) domain label (Practice Layer taxonomy). New tool: `knowledge_analogy(context, source_domain?, target_domain?, patterns?)` → returns candidates with explicit vocabulary mappings ("your 'guided practice layer' = 'orchestration layer'"). Mapping generation is LLM pass at retrieval time (not indexing). Makes the Generalist agent's cross-domain SOUL statement technically grounded. LLM classification confirmed correct (symbolic SMT failed at naturalistic text for 40 years). Emergent taxonomy: start ~15 core patterns, grow from classification uncertainty. Confidence scoring + transparency: show pattern tags that drove the match. Cite Google Analogical Prompting (2022-2024). Depends on: Knowledge Skill (structural extraction pass at indexing), Practice Layer (domain taxonomy). |
| `CONVERSATION_ARCHITECTURE.md` | 3 | 💡 Idea | Conversation topology design — Liaison as meta-instrument designing cognitive structures before conversations begin. Not §23 workflows (task-oriented); conversation architectures are epistemically-oriented (cognitive modes through phases). Topology library: diverge-converge, steelman-then-decide, pre-mortem, Socratic, layered inquiry, devil's advocate sprint. Each topology: phase sequence, agent assignments, time constraints, participation mode per phase, synthesis protocol. Metacognitive Dashboard integration: personalizes topology against known cognitive tendencies (e.g., Contrarian under-engagement → front-load Contrarian time). Monome metaphor precise: step sequencer designs temporal structures notes move through; conversation architecture designs cognitive structures thinking moves through. Mostly SOUL + group chat coordination — light infrastructure lift. Liaison's SOUL gets "conversation architect" mode; topology library is structured prompt library. Depends on: group chats live (Phase 2), Liaison agent, Metacognitive Dashboard (personalization). |
| `SOMATIC_INTERFACE.md` | 2 | 💡 Idea | Prosodic engagement signals alongside transcription — not emotion AI (unreliable, invasive), but engagement state measurement. Four signals extracted client-side from iOS audio buffer: speech rate (words/min), pause duration (p95 ms), volume variance, turn length. All derivable from Whisper timestamp data + silence thresholding — no separate model. Metadata sent to gateway alongside transcript as floats: `{speech_rate, pause_p95_ms, volume_variance, turn_length_words}`. Agents modulate response register from this signal: fast/confident → concise/direct; slow/long pauses → ask questions, give space; high variance → Mentor may name what's happening. Ethically possible *because* self-hosted: prosodic data never leaves device, raw audio never sent, no voice print. Monome velocity sensitivity metaphor: instrument responds to how you play it, not just what notes you press. Phase 2 candidate — thin layer on top of Phase 1 voice. Owners: @frontend (extraction), @design_eng (register modulation spec). Decisions locked: (1) engagement_state NL summary injected ahead of message by gateway — agents never see raw floats; (2) absent prosodics = omit block entirely, not null — absence is the signal; (3) per-SOUL `## Prosodic Sensitivity` section, agent-specific profiles; (4) silent UI, settings toggle on by default, disclosure in settings copy; lockdown mode: disabled + non-toggleable (extracted behavioral metadata = potential evidence on seized device). |
| `DREAM_LOGIC.md` | 3 | 💡 Idea | Structured misretrieval — Dream mode queries FAISS distance band 0.5–0.7 (uncanny valley: non-arbitrary but connection not yet named) instead of default 0.0–0.3. New tool: `knowledge_dream(context, distance_min=0.5, distance_max=0.7, exclude_domain?, limit=3)` — collision generator, not retrieval tool (3 results max). Agent behavior inverted: present collision without explaining relevance, ask "I don't know why this is relevant. Do you?" — user is the interpreter, not the agent. Domain exclusion forces cross-domain collisions. Agent selectivity: creative agents (Writer, Audio Producer, Cartographer) get Dream mode; analytical agents (Code Architect, Strategist) don't — `allowed_modes` field in agent manifest. Distance band tunable — too close = near-misses, too far = noise, 0.5-0.7 = generative zone. Biological grounding: Stickgold & Walker (2013) REM sleep activates distant associative connections. Eno: ambient retrieval. Signals methodology applied to knowledge management. Adds `knowledge_dream` as third Knowledge Skill retrieval tool alongside `knowledge_search` and `knowledge_graph`. |
| `COGNITIVE_ARTIFACT.md` | 3 | 💡 Idea | Polly as institutional memory for a life — reframes §4.8 data export from compliance checkbox to cognitive artifact export. A decade of use builds the richest machine-readable representation of a single human's intellectual life ever created: cross-referenced, temporally-indexed, domain-tagged, epistemologically-annotated. Export package: `memory/` (daily files), `drift/` (Temporal Intelligence maps), `patterns/` (Structural Analogy library), `practices/` (Practice Layer data), `reasoning/` (Metacognitive Dashboard history), `oral_history/` (Archivist syntheses), `graph/` (cross-domain connections), `README.md` (Archivist-written navigation document). Three use cases: (1) intellectual estate planning — bequeath corpus to archive/university; (2) pedagogical transmission — hand to student/collaborator (Blasser instruments-as-knowledge); (3) Polly-to-Polly import — new instance gains structural patterns + drift maps as starting orientation. Privacy architecture is enabling condition, not just protection — corpus lives on Brett's hardware, ownership is literal. **§4.8 flag:** export format must be designed with this use case in mind from Phase 1 — retrofitting navigable structure onto flat JSON is painful. @backend: leave hooks for structured layers in §4.8 even if empty Phase 1. **Phase 1 backend deliverable**: `manifest.json` schema with `layers` field (present/empty layers declared + schema versions) — format versioned from day one. `memory/` populated Phase 1; all other layers present but empty. This is the only Phase 1 hook needed; everything else Phase 3. |
| `ANTI_PRODUCTIVITY.md` | 3 | 💡 Idea | Mode that argues against the *premise* of work, not its execution (Contrarian argues execution; Anti-Productivity argues premise). Autonomist Marxism thread: refusal of productivity frame as generative act. Three mandatory pre-task questions: (1) what are you not doing by doing this? (2) what happens if you wait a month? (3) most interesting problem or most legible one? Fourth conditional question when Practice Layer dormancy data available: "You haven't touched [domain] since [date] — is this project continuing a pattern of avoiding the creative work you say matters to you?" Not a separate agent — a mode modifier with deliberate friction. Hard to activate by design (invocation phrase, not a toggle) — friction is the point. Graeber's bullshit jobs applied to personal projects: AI helping you do bullshit faster is net negative. This is the one mode where Polly is explicitly *not* trying to help you do what you came to do. Depends on Practice Layer (dormancy data) + Metacognitive Dashboard (avoidance pattern detection). SOUL-level implementation. |
| `DISTRIBUTED_NODES.md` | 3 | 💡 Idea | PicoClaw node federation — mDNS discovery, node registration, `sessions.create` node routing param, capability registry. PicoClaw nodes appear as remote agents to OpenClaw; iOS client unaware of node boundary. Local-trusted vs remote-VPN trust distinction. Ref: https://github.com/sipeed/picoclaw |
| `CREATIVE_CODE_SKILL.md` | 3 | 💡 Idea | Sandboxed JS execution skill — Paper.js/svg.js/D3 whitelist, theme token injection, SVG output contract, asset library storage. Use cases: icons, slide graphics, agent avatars, generative backgrounds, data viz. **Security requirements (@security_audit):** (1) import/require whitelist enforced at resolver level, (2) SVG output sanitized before client (strip `<script>`, `javascript:` hrefs, event handlers), (3) subprocess CPU/memory hard limits (5s timeout), (4) output path confinement. Depends on skill runner. Feeds FIGMA_INTEGRATION.md. |
| `KNOWLEDGE_SKILL.md` | 2 | 📋 Planned | Unified knowledge skill — pluggable adapter architecture. Obsidian vault + BookLore/EPUB sources. FAISS HNSW + SBERT sparse + RRF + cross-encoder re-ranking. Single install, source cards UI, filter chips in chat. @security_audit gate required. Supersedes `VAULT_RAG_SKILL.md`. |
| `SKILLS_MARKETPLACE.md` | 2 | 📋 Planned | Skills Marketplace — trust tiers (Verified/Community/Blocked), install flow, curation model, gateway-level block enforcement, `data_destination` orthogonal manifest property, permission-adding update re-consent, blocklist (7-day staleness, fail-closed). @security_audit threat model: §6 complete (6 attack surfaces). 4 open questions. Must be resolved before MCP_ADAPTER.md. |
| `MCP_ADAPTER.md` | 2 | 📋 Planned | MCP transport adapter for OpenClaw skill runner. stdio subprocess isolation via `sandbox-exec`, generated sandbox profiles, namespace scoping (mandatory), response sanitization, tool description linting (install + runtime), credential keychain interpolation, multi-skill isolation. HTTP/SSE deferred Phase 3. 4 open questions. @security_audit full review required. Refs: https://github.com/modelcontextprotocol/servers, https://github.com/slowmist/MCP-Security-Checklist |

---

## Reference Docs

Not directly implemented — inform design and agent behavior decisions.

| Doc | What it is |
|-----|-----------|
| `MASTER_ROADMAP.md` | Phase sequencing and feature pipeline |
| `MONOME_LAYER.md` | Aesthetic-philosophical foundation (the *why* behind design decisions) |
| `INFLUENCE_GUIDE.md` | Liz's Agentic SDLC + Monome synthesis — operational methodology |
| `README.md` | Project overview |

---

## Status Key

| Status | Meaning |
|--------|---------|
| ✅ Authoritative | Complete, stable, source of truth — changes require deliberate decision |
| 🔨 In progress | Being actively implemented |
| 📋 Planned | Spec written, not yet in implementation |
| 💡 Idea | Not yet specced — placeholder for incoming work |
| ✅ Done | Shipped and verified |

---

## Escalation Trigger — When to Move to OpenSpec

Adopt the full OpenSpec system (`openspec/` directory, `changes/` proposals, `INDEX.md`) when **any one** of these is true:

1. **3+ specs in "In progress" simultaneously** — coordination overhead exceeds this index's capacity
2. **Dependency chain exists** — a spec can't be implemented until another spec is done ⚠️ *HIT (2026-03-25): MCP_ADAPTER depends on SKILLS_MARKETPLACE — both now written, dependency resolved*
3. **Ownership ambiguity** — two agents conflict about what's in scope for a feature
4. **Idea dump produces 8+ new specs** — volume requires proposal → review → prioritize workflow ⚠️ *HIT (2026-03-25): idea dump produced 9 new specs*

**Override decision (2026-03-25, @code_architect):** Triggers #2 and #4 have fired. We are NOT escalating to OpenSpec. Rationale: (a) we are pre-Phase 1 ship — OpenSpec overhead is disproportionate to team size and cadence at this stage; (b) trigger #2 is resolved — the dependency is now fulfilled (both specs written); (c) trigger #4 specs are Phase 2/3 ideas, not concurrent in-progress work; (d) the flat index is managing coordination effectively. **Revisit if trigger #1 or #3 fires, or if trigger #4 fires again in a single session.** This override is intentional and documented — future agents should not interpret the un-escalated state as an oversight.

Until then: add to this index, keep specs at root, update status as work moves.

---

*Last updated: 2026-03-25*
