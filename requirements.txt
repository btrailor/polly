# Apollo Dependencies

# Internal libraries (editable install from repo root)
-e libs/polly-routing

# Core
pyyaml>=6.0
pydantic>=2.0

# Vector Database
chromadb>=0.4.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# HTTP Client
httpx>=0.24.0
aiohttp>=3.9.0  # For async HTTP in integrations

# AI Providers (Phase 11)
anthropic>=0.20.0  # Claude models
openai>=1.10.0  # GPT models
litellm>=1.30.0  # Unified LLM API proxy for 100+ providers

# Security & Secrets (Phase 11)
keyring>=24.0.0  # Secure storage for API keys
cryptography>=41.0.0  # Encryption for fallback storage

# Utilities
watchdog>=3.0.0  # File watching
rank-bm25>=0.2.2  # BM25 keyword search for hybrid RAG
spacy>=3.7.0  # NLP for pattern learning
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl  # spaCy English model

# Compression (Wave 1, Task 13)
llmlingua>=0.2.0  # Fast algorithmic prompt compression (2x-10x token reduction)

# Adaptive Memory Layer (Phase 1.5)
mem0ai>=1.0.0  # Graph-based memory with entity relationships and multi-level context

# Token Counting (Scalable Memory Layers)
tiktoken>=0.5.0  # Accurate token counting for budget allocation

# LlamaIndex Knowledge Graph (Wave 5, Task 24)
llama-index-core>=0.11.0,<0.12.0        # VectorStoreIndex, SubQuestionQueryEngine, PropertyGraphIndex
llama-index-vector-stores-chroma>=0.2.0  # ChromaVectorStore wrapping existing collections
llama-index-embeddings-ollama>=0.3.0     # Ollama embedding adapter (must match RAG embedding model)
llama-index-llms-litellm>=0.2.0          # LiteLLM adapter for LlamaIndex LLM calls

# Optional: For enhanced code parsing
# tree-sitter>=0.20.0
# tree-sitter-python
# tree-sitter-javascript
# tree-sitter-lua
